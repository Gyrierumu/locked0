# Scripts operacionais

Seeds, importadores e rotinas como criação de administradores devem viver aqui e executar fora do runtime da aplicação. Cada script futuro deve validar as próprias variáveis de ambiente e ser exposto por um comando explícito no `package.json`.
