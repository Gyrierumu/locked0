# Progress

Responsável por conclusões do usuário, progresso de conquistas, guias e checklists, além da última posição.

Pode consumir contratos públicos de `catalog` e `guides`. Esses módulos nunca dependem de `progress`. As tabelas ficam em `src/db/schema/progress.ts`.
