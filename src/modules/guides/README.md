# Guides

Responsável por guias, alvos, passos, seções, nós de conteúdo, checklists, versões, publicação e snapshots.

Pode consumir contratos públicos de `catalog`, mas suas regras centrais não conhecem `progress`. As tabelas editoriais ficam em `src/db/schema/editorial.ts`.
